if __name__ == '__main__':
    x = input()
    print(any(a.isalnum() for a in x) )
    print(any(a.isalpha() for a in x) )
    print(any(a.isdigit() for a in x) )
    print(any(a.islower() for a in x) )
    print(any(a.isupper() for a in x) )
